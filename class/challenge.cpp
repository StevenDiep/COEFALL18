#include<iostream>
#include<vector>
using std::cin;
using std::cout;
using std::endl;
using std::vector;

vector<int> combineV(vector<int> A, vector<int> B)
{
	vector<int> C = (vector<int> A) + (vector<int> B)
}

int main(){
vector<int> Vector1(10,1);
vector<int> Vector2(10,2);
vector<int> Vector3 = combineV(vector<int> Vector1, vector<int> Vector2)
cout<< 
